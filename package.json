{
  "name": "drink-explorer",
  "version": "1.1.1",
  "type": "module",
  "scripts": {
    "dev": "servor --reload",
    "start": "servor --reload",
    "build": "mkdir -p dist && cp -r *.html *.css *.js src drinks.json dist/",
    "deploy": "npm run build"
  },
  "dependencies": {
    "servor": "^4.0.2"
  }
}